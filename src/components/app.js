import React, { Component } from 'react';

export default class App extends Component {
  render() {
    return (
      <div>React simple starter</div>
    );
  }
}

//  react router, you dont make a request. intercepts the change in the url, manually look in the url and display different components
//spas -- single page applications --> no longer navigating between htmls docs created by a remote web server. one html doc, and javascript
// changes the components that are showing 
